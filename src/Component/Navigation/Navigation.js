import React from 'react';

const Navigation = () => {
    return (
        <div style = {{display:'flex',justifyContent:'flex-end'}}>
            <nav className='f3 link dim underline pa3 pointer'>
                <p>Sign Out</p>
            </nav>
        </div>
    );
};

export default Navigation